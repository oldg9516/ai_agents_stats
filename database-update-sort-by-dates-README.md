# Database Update: Sort by Dates DESC

## Проблема

В таблице на дашборде данные должны сортироваться по датам от новых к старым внутри каждой категории, но сортировка применялась только на клиенте к текущей странице (50 записей). При server-side пагинации это не работает корректно.

## Решение

Обновлена SQL функция `get_detailed_stats_paginated` для правильной сортировки на сервере:

```
ORDER BY:
1. category ASC          - категории в алфавитном порядке
2. sort_order ASC        - строка версии-уровня (1) перед недельными строками (2)
3. week_start_for_sort DESC NULLS FIRST - недели от новых к старым, NULL (версия) первым
```

## Как применить

### Вариант 1: Через Supabase Dashboard (рекомендуется)

1. Открой [Supabase Dashboard](https://supabase.com/dashboard)
2. Выбери свой проект
3. Перейди в **SQL Editor** (левая панель)
4. Нажми **New Query**
5. Скопируй весь код из файла `database-update-sort-by-dates.sql`
6. Вставь в редактор и нажми **Run** (или Cmd/Ctrl + Enter)
7. Проверь, что функция обновилась без ошибок

### Вариант 2: Через Supabase CLI

```bash
# Убедись, что установлен Supabase CLI
supabase --version

# Примени миграцию
supabase db push

# Или выполни SQL напрямую
psql $DATABASE_URL < database-update-sort-by-dates.sql
```

## Изменения в коде

### SQL функция (Supabase)
- ✅ Добавлено поле `week_start_for_sort` для правильной сортировки
- ✅ `ORDER BY` изменен на: `category ASC, sort_order ASC, week_start_for_sort DESC NULLS FIRST`

### React компонент (detailed-stats-table.tsx)
- ✅ Убрана начальная client-side сортировка (теперь пустой массив `sorting = []`)
- ✅ Добавлен комментарий, что основная сортировка происходит на сервере
- ✅ Оставлена возможность пользователю вручную пересортировать текущую страницу через UI

### Кастомная функция сортировки для колонки dates
- ✅ Оставлена в коде для корректной client-side сортировки, если пользователь кликнет на заголовок колонки
- ✅ Извлекает первую дату из строки "YYYY-MM-DD - YYYY-MM-DD"
- ✅ NULL значения (версия-уровень) всегда идут первыми

## Результат

После применения скрипта данные в таблице будут отображаться так:

```
📊 Категория A
   ├─ v2 (версия-уровень)          [sortOrder=1, dates=NULL]
   ├─ 2025-02-01 - 2025-02-07      [самая новая неделя]
   ├─ 2025-01-25 - 2025-01-31
   └─ 2025-01-18 - 2025-01-24      [самая старая неделя]

📊 Категория B
   ├─ v3 (версия-уровень)
   ├─ 2025-02-01 - 2025-02-07
   └─ ...
```

## Проверка

После применения скрипта:

1. Открой дашборд: `http://localhost:3000/dashboard`
2. Проверь, что в таблице:
   - Категории отсортированы по алфавиту
   - В каждой категории первая строка - версия-уровень (без дат)
   - Недельные строки идут от новых к старым
3. Переключи страницы пагинации - сортировка должна сохраняться

## Откат изменений

Если нужно вернуть старую версию функции, сохрани текущую версию перед применением:

```sql
-- Получить текущую версию функции
SELECT pg_get_functiondef(oid)
FROM pg_proc
WHERE proname = 'get_detailed_stats_paginated';
```

## Примечания

- SQL функция использует `NULLS FIRST` для PostgreSQL (Supabase)
- Клиентская сортировка все еще доступна - пользователь может кликнуть на заголовок колонки
- Пагинация остается server-side (50 записей на страницу)
- Фильтры (search, date range, version, category, agent) работают как раньше
