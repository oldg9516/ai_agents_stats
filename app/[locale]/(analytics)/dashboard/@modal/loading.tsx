/**
 * Loading state for @modal slot
 * Returns null to prevent showing loader when modal is loading
 * The modal itself will handle its own loading state
 */
export default function Loading() {
	return null
}
